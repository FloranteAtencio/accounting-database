services:
  postgres:
    image: postgres:15
    container_name: feature_env
    restart: always
    env_file:
      - ../.env.dev
    ports:
      - "5432:5432"   # 🔒 LOCAL ONLY
    volumes:
      - dev_pgdata:/var/lib/postgresql/data
      - ../conf/postgres.conf.dev:/etc/postgresql/postgresql.conf
      - ../conf/pg_hba.conf.dev:/etc/postgresql/pg_hba.conf
    command:
      - "postgres"
      - "-c"
      - "config_file=/etc/postgresql/postgresql.conf"
      - "-c"
      - "hba_file=/etc/postgresql/pg_hba.conf"
volumes:
  dev_pgdata:
